import React from 'react';

const Quote = () => {
    return(
    <div>
        "This is a quote that I am using right now just as a placeholder until I have something else to show for my work"
    </div>
    );
}

export default Quote;